package Tema_4;

public class Ejercicio_01 {

	public static void main(String[] args) {
		
		int rate, amt, calc, base, lugar, MADRID, VALENCIA, BARNA, MAD_RATE, BCN_RATE, VAL_RATE, points;
		
		
		
		
		if (lugar == MADRID) {
			rate = MAD_RATE;
			amt = base * MAD_RATE;
			calc = 2 * basis (amt) + extra(amt) * 1.05;
		}
		
		else if (lugar == BARNA) {
			rate = BCN_RATE;
			amt = base * rate;
			points = 2;
			calc = 2 * basis (amt) + extra(amt) * 1.05;
		}
		else if (lugar == VALENCIA){
			rate = 1;
			amt = base * rate;
			calc = 2 * basis (amt) + extra(amt) * 1.05;
		}
		else {
			rate = 1;
			amt = base;
			calc = 2 * basis (amt) + extra(amt) * 1.05;
		}
	}

}
